#include <iostream>

using namespace std;

int main(){
	/*Codificare un programma completo in linguaggio C++ in cui:
	• l’utente deve inserire da tastiera il capitale iniziale (C), il
	tasso di interesse (TI), il target (T) e numero di anni (N);
	• se uno tra T ed N `e un numero minore o uguale a zero,
	allora il programma non dovr`a tener conto di tale
	parametro; nel caso in cui sia T che N siano minori o
	uguali a zero allora il programma terminer`a con un messaggio di errore*/
	
	double capitale,target,interesse	int anni;
	cout<<"ins. capitale: ";cin>>capitale;
	cout<<"ins. target: ";cin >>target;
	cout<<"ins: anni: ";cin anni;
	cout <<"isn. tasso di interesse";cin>>interesse
	if (target<=0 || numero di anni<=0)
	{
		
	}
	else
	{
		cout<<"valori errati riprovare";
	}
}
	